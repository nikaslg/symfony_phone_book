<form method="post" action="{{ path('phone_book_delete', {'id': phone_book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone_book.id) }}">
    <button class="btn">Delete</button>
</form>
